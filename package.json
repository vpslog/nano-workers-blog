{
  "type": "module",
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^9.0.0"
  },
  "scripts": {
    "build": "node build-index.js",
    "deploy": "node build-index.js && wrangler deploy"
  }
}
